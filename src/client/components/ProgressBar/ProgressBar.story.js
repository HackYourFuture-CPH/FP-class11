import React from "react";
import { storiesOf } from "@storybook/react";
import ProgressBar from "./Progressbar.componenet";

storiesOf("Progress Bar", module).add("Crop Progress Bar", () => (
  <ProgressBar />
));
