import React from 'react';

import UpdateDateRange from './update-date-range.component';

export default { title: 'updaterange' };

export const renderDateRange = () => (
  <UpdateDateRange date="date" text="Update Range" />
);
